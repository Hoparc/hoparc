fragment service on Service {
  image {
      asset {
        url
      }
    }
  name
  slug {
    current
  }
  category {
    name
    slug {
      current
    }
  }
  description
}

query allServices {
    allService {
        ...service
    }
}